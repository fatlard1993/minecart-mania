package justfatlard.minecart_mania.rail;

import net.minecraft.world.level.block.RailBlock;

/** A plain rail made of planks: vanilla's speed, for the line that only needs to get there. */
public class WoodenRailBlock extends RailBlock {
	public WoodenRailBlock(Properties properties) {
		super(properties);
	}
}
